<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.TemplateMapper">
	
	<!-- タスクの全件取得 -->
	<select id="findAll" resultType="com.example.demo.entity.Template">
        SELECT * FROM template where deletedAt IS NULL;
    </select>

    <select id="sort1" resultType="com.example.demo.entity.Template">
        SELECT * FROM template where typeId = 1 AND deletedAt IS NULL;
    </select>

    <select id="sort2" resultType="com.example.demo.entity.Template">
        SELECT * FROM template where typeId = 2 AND deletedAt IS NULL;
    </select>

    <select id="sort3" resultType="com.example.demo.entity.Template">
        SELECT * FROM template where typeId = 3 AND deletedAt IS NULL;
    </select>
    
    <!-- 新規タスクの登録 -->
    <insert id="save">
        INSERT INTO template
            (typeId, template, createdAt, updatedAt)
        VALUES
            (#{typeId}, #{template}, CURRENT_TIMESTAMP,CURRENT_TIMESTAMP);
    </insert>
    
        <!-- タスクIDを指定してタスクを取得 -->
    <select id="getTemplate" resultType="com.example.demo.entity.Template">
        SELECT * FROM template where id = #{id} AND deletedAt IS NULL;
	</select>
	
	   <!-- タスクの削除 -->
   <delete id ="delete">
	   UPDATE template SET deletedAt = CURRENT_TIMESTAMP, deleteFlg = 1 WHERE id = #{id}; 
       
   </delete>
   
      <!-- タスクの更新 -->
   <update id="update" parameterType="com.example.demo.entity.Template">
	   UPDATE template SET  template = #{template}, typeId = #{typeId}, updatedAt = CURRENT_TIMESTAMP WHERE id = #{id} and updatedAt = #{updatedAt}; 
   </update>
    
</mapper>
